{# templates/login/index.html.twig #}
{% extends 'base.html.twig' %}

{# ... #}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-4">
            </div>
            <div class="col-4">
                <h1 class="titre text-center">Mon Profil</h1>
            </div>
            <div class="col-4">
            </div>
        </div>
        <div class="row">
            <div class="col-4">
                <label for="email">Email :</label>
                <input id="email" class="form-control" type="email" value="{{ user.email }}" disabled>
            </div>
        </div>
        <div class="row mt-2">
            <div class="col-2">
                <a class="btn btn-secondary" href="{{ path('app_accueil') }}">Retour</a>
            </div>
        </div>
    </div>
{% endblock %}